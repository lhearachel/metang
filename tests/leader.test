--leader LEADER
# input
Bulbasaur
Ivysaur
Venusaur
Charmander
Charmeleon
Charizard
Squirtle
Wartortle
Blastoise
# output
/*
 * This file was generated by metang; DO NOT MODIFY IT!!
 * Source file: stdin
 * Program options:
 *   --leader LEADER
 */
#ifndef METANG_STDOUT
#define METANG_STDOUT

#ifdef METANG_ENUM

#undef METANG_DEFS /* Do not allow also including the defs section */

enum stdin {
    LEADER_BULBASAUR = 0,
    LEADER_IVYSAUR = 1,
    LEADER_VENUSAUR = 2,
    LEADER_CHARMANDER = 3,
    LEADER_CHARMELEON = 4,
    LEADER_CHARIZARD = 5,
    LEADER_SQUIRTLE = 6,
    LEADER_WARTORTLE = 7,
    LEADER_BLASTOISE = 8,
};

#endif // METANG_ENUM

#if defined(METANG_DEFS) || !defined(METANG_ENUM)

#define LEADER_BULBASAUR 0
#define LEADER_IVYSAUR 1
#define LEADER_VENUSAUR 2
#define LEADER_CHARMANDER 3
#define LEADER_CHARMELEON 4
#define LEADER_CHARIZARD 5
#define LEADER_SQUIRTLE 6
#define LEADER_WARTORTLE 7
#define LEADER_BLASTOISE 8

#endif // defined(METANG_DEFS) || !defined(METANG_ENUM)

#ifdef METANG_LOOKUP

struct stdin_entry {
    const long value;
    const char *def;
};

const struct stdin_entry stdin_lookup[] = {
    { LEADER_BLASTOISE, "BLASTOISE" },
    { LEADER_BULBASAUR, "BULBASAUR" },
    { LEADER_CHARIZARD, "CHARIZARD" },
    { LEADER_CHARMANDER, "CHARMANDER" },
    { LEADER_CHARMELEON, "CHARMELEON" },
    { LEADER_IVYSAUR, "IVYSAUR" },
    { LEADER_SQUIRTLE, "SQUIRTLE" },
    { LEADER_VENUSAUR, "VENUSAUR" },
    { LEADER_WARTORTLE, "WARTORTLE" },
};

#endif // METANG_LOOKUP

#endif // METANG_STDOUT
